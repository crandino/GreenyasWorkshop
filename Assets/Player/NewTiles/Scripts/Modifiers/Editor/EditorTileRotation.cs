using Hexalinks.Tile;
using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EditorTileRotation : TileRotation
{
    public EditorTileRotation(Tile tile, float rotationTime = 0.5F, Action onStartRotation = null, Action onFinishRotation = null) : base(tile, rotationTime, onStartRotation, onFinishRotation)
    {
    }


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
